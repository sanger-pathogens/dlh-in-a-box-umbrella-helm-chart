This directory contains the static browser assets for `platformHome`.

- `keycloak.js` is the vendored Keycloak browser adapter module used by the
  launchpad so the portal does not depend on a server-side `/js/keycloak.js`
  path.
