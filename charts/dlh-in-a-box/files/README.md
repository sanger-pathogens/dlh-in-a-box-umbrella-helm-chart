This directory contains chart-owned static assets packaged into rendered Kubernetes objects.

- `platform-home/` stores the browser launchpad assets that are embedded into the
  `platform-home` ConfigMap at render time.
