apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hive.fullname" . }}-init-config
  namespace: {{ .Release.Namespace }}
data:
  POSTGRES_HOST: {{ include "hive.postgresHost" . | quote }}
  POSTGRES_PORT: {{ include "hive.postgresPort" . | quote }}
