apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hive.fullname" . }}-init-config
  namespace: {{ .Release.Namespace }}
data:
  POSTGRES_HOST: {{ .Values.postgres.host | quote }}
  POSTGRES_PORT: {{ .Values.postgres.port | quote }}