{{- /*
This chart's own default: exactly one metastore, no name override. A parent
composing several named instances (e.g. one per catalog) calls
"hive.metastoreInstance" itself instead, once per name, via .Subcharts.hive
-- see _metastore.tpl.
*/ -}}
{{- if .Values.metastore.enabled }}
{{- include "hive.metastoreInstance" (dict "context" $) }}
{{- end }}