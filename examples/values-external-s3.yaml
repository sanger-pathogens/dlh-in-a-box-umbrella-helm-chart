global:
  storage:
    backend: externalS3
    s3:
      endpoint: https://s3-compatible.example.net
      region: us-east-1
      bucket: lakehouse-shared
      pathStyleAccess: true
      existingSecret: lakehouse-object-store-credentials

minio:
  enabled: false
