global:
  identity:
    directory:
      mode: keycloakLocal
      ldap:
        enabled: false
    external:
      clients:
        trino:
          enabled: false
        trinoDirectGrant:
          enabled: false
    provider:
      keycloak:
        registration:
          enabled: false
