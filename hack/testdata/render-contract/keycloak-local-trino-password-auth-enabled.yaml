global:
  identity:
    directory:
      mode: keycloakLocal
      ldap:
        enabled: false
    external:
      clients:
        trino:
          enabled: true
          passwordAuthEnabled: true
          passwordAuthMode: ldap-file
